<%= simple_form_for @opcional, :html => { :class => 'form-vertical' } do |f| %>
  <%= f.input :nome %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                opcionais_path, :class => 'btn btn-default' %>
<% end %>
